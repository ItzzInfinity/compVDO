# Manual Tasks (require your action)  (aliased as `manualTasks.md`)

Everything that needs an account, an API key, a console click, a device, or a
password lives here. The agent **cannot** do these and must never retry them —
it files an `M<n>` and moves on to work that isn't blocked.

Roadmap items waiting on one of these are marked `[M]` and name the number.

## Pending

### M1 — Supply real phone clips for the trial  (unblocks 1.16, 2.8)
- [x] 1. Copy 3–5 videos straight off your phone into a folder, e.g.
      `~/Videos/compvdo-samples/`. Do **not** let a messaging app re-compress
      them — copy over USB or MTP.
- [x] 2. Include at least one **portrait** clip (this is what catches rotation
      bugs) and one that is not MP4 if you have one.
- [x] 3. Tell me the folder path.
- Until then, Phase 1 is tested against synthetic clips generated by ffmpeg,
  which prove correctness but not real-world compression ratios.

### M2 — Windows machine for Phase 4  (unblocks 4.4, 4.5)
- [ ] 1. A Windows 10/11 machine or VM with Python 3.10+ installed.
- [ ] 2. Confirm whether you want a single `.exe` or a folder build
      (folder starts faster; single file is easier to hand to someone).
- Not needed until Android (Phase 3) is done — you asked for Android first.

### M3 — Android build environment for Phase 3  (unblocks 3.6)
- [ ] 1. Android Studio installed, or a JDK 17 + command-line SDK tools.
- [ ] 2. A physical device with USB debugging on (emulators have unreliable
      hardware video encoders — this matters for this app specifically).
- [ ] 3. Minimum Android version you want to support (Media3 Transformer is
      solid from API 26; API 33+ gives the nicer photo-picker permission).
- **This is the next thing that will block me.** Phases 1 and 2 (Linux CLI and GUI) are complete.

### M4 — Decisions I need from you  (unblocks nothing; changes defaults)
- [x] 1. **The lossless question** — **decided 2026-09-20: keep visually-lossless
      HEVC as the default**, with true lossless remaining the opt-in `archive`
      mode. No code change needed; this is what ships.
- [ ] 2. Anything here you want dropped from v1 to ship sooner.

## Your issues & suggestions
<!-- Add anything here and I will pick it up on the next pass. -->
- [x] Read `./dev_guide.md` and follow the norms — **Done 2026-09-20.** Applied
      §3.2 module contracts, §11 console vocabulary (`[TX] [RX] [INFO] [WARN]
      [ERR]`) through one `_log()`, §7.5 a single `_set_busy()` cleared on every
      terminal path, §12 a delete confirmation naming the targets with Cancel
      defaulted, and §4 pinned requirements files. Root cause of the biggest gap:
      the GUI had **no confirmation at all** before trashing originals — §12 is
      what caught it. The tab machinery in §2/§4 does not apply, as you said.
      (`dev_guide.md` moved to `docs/` and stays gitignored, as your `.gitignore`
      already had it.)
- [x] for all processes Limit the CPU to `n-2` cores — **Done 2026-09-20.**
      `compvdo/cpu.py`; default `n-2`, `--cores N` to override, shown by
      `compvdo caps`. Root cause of the non-obvious part: `-threads` alone does
      **not** bound x265, which runs a private pool sized from the machine and
      ignores it — `pools=` in `-x265-params` is the actual knob. Measured
      215% CPU at `--cores 2` and 750% at `--cores 10`. Affinity is read with
      `os.sched_getaffinity` so a container slice is not oversubscribed.
- [x] Move the repos into /docs and create well pictured readme — **Done
      2026-09-20.** Root is now `README.md` plus two symlinks; everything else
      lives in `docs/`. Root cause of the wrinkle: `scaffold.mjs` hard-codes
      `<dir>/docs/roadmap.md`, so `--dir=docs` would have sent it looking for
      `docs/docs/`. Two root symlinks (`FSD.md`, `manual-task.md`) give the
      layout you picked **and** keep every scaffold command working with no
      flag. Your four original names live on as `docs/Architecture.md`,
      `docs/ToDo.md`, `docs/memory.md`, `docs/manualTasks.md`.

## Completed
<!-- Move finished items down here with the date. -->
