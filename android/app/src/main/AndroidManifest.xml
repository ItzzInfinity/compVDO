<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Read videos from shared storage.

      API 33+: READ_MEDIA_VIDEO. This covers every row MediaStore has filed as
      video, anywhere on external storage — including
      Android/media/com.whatsapp/... . It deliberately does NOT cover rows whose
      media_type is not video (a video sitting in Download/ that the download
      manager filed as application/octet-stream, or a WhatsApp "Documents"
      item); MediaProvider filters those out of our cursors.

      API <= 32: READ_EXTERNAL_STORAGE is the legacy blanket read and DOES cover
      those non-media rows, so the Download/documents case resolves itself
      below 33.

      The remaining API 33+ gap is closed with a Storage Access Framework tree
      grant (SafVideoScanner) — the same route needed for any folder hidden by
      a .nomedia marker. MANAGE_EXTERNAL_STORAGE is intentionally NOT declared:
      it is a Play-policy landmine and SAF is the sanctioned route.
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- Foreground service for long encodes that survive screen-off -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".CompVdoApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.CompVDO">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.CompressionService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
